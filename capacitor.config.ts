import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.montdrive',
  appName: 'mont-drive-2',
  webDir: 'www',
  version: '1.0.1'
};

export default config;
